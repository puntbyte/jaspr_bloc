/// Shared bloc library for cross-platform state management.
///
/// Contains pure-Dart cubits and blocs that can be used unchanged
/// in both Flutter apps (via `flutter_bloc`) and Jaspr web apps
/// (via `jaspr_bloc`).
library;

export 'src/counter/counter_cubit.dart';
